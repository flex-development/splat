/**
 * @file Fixtures - cc
 * @module fixtures/cc
 */

/**
 * Character classes.
 *
 * @enum {string}
 */
enum cc {
  alnum = 'alnum',
  alpha = 'alpha',
  ascii = 'ascii',
  blank = 'blank',
  cntrl = 'cntrl',
  digit = 'digit',
  graph = 'graph',
  lower = 'lower',
  print = 'print',
  punct = 'punct',
  space = 'space',
  upper = 'upper',
  word = 'word',
  xdigit = 'xdigit'
}

export default cc
