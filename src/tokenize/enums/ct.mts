/**
 * @file Enums - ct
 * @module splat/tokenize/enums/ct
 */

import type { ContentType } from '@flex-development/fsm-tokenizer'

/**
 * Content types.
 *
 * @enum {ContentType}
 */
enum ct {
  expression = 'expression',
  pattern = 'pattern'
}

export default ct
